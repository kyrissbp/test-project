const array=[1,2,10,16],double=[],newArray=array.forEach(r=>{double.push(2*r)});console.log("foreach",double);const mapArray=array.map(r=>2*r);console.log("map",mapArray);const filterArray=array.filter(r=>r>5);console.log("filter",filterArray);const reduceArray=array.reduce((r,a)=>r+a,0);console.log("reduced",reduceArray);